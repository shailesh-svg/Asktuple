# Gaugetuple API that the MCP server reads truth from
GAUGETUPLE_API_BASE=https://dev.gaugetuple.com
GAUGETUPLE_API_TOKEN=

# Asktuple shell -> host gateway
VITE_ASKTUPLE_GATEWAY=http://localhost:8787
